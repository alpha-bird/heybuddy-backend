machine:
  pre:
    - curl -sSL https://s3.amazonaws.com/circle-downloads/install-circleci-docker.sh | bash -s -- 1.10.0
    - pip install docker-compose
    - pyenv rehash
  services:
    - docker

dependencies:
  override:
    - echo "Ignoring default CircleCI dependencies"

test:
  override:
    - echo "Skipping tests" # docker-compose run test

deployment:
  hub:
    branch: master
    commands:
      - docker login -e benjamin-wildebeestdev@waldher.us -u wildebeestcircleci -p ':kW?j@PNe?lft;TQ9*z*[QF3tFrzy[.7'
      - docker push wildebeestdev/troy-bilt
